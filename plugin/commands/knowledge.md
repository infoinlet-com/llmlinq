---
description: Search this user's own LLMLinq knowledge
argument-hint: [what to look for]
---

Search the user's LLMLinq knowledge for: $ARGUMENTS

Use the `search_knowledge` tool. If nothing matches, say so plainly rather than
answering from general knowledge as though it came from their notes.

Report each result with its breadcrumb and document title so the user can see
where it came from, and offer to `fetch_knowledge` any document they want in
full.
